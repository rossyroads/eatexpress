{
    "name": "eatexpress",
    "version": "1.0.0",
    "license": "ISC",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "bash -c \"set -a && source /workspace/.env && set +a && ./gradlew bootRun\"",
        "clean": "./gradlew clean",
        "format-check": "prettier --check .",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "prettier": "^3.6.2",
        "prettier-plugin-java": "^2.7.5",
        "prettier-plugin-sql": "^0.19.2"
    }
}
